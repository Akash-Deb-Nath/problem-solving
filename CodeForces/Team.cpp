#include <iostream>

using namespace std;

int main()
{
    int n, a, b, c, i, r = 0;
    cin >> n;
    for (i = 1; i <= n; i++)
    {
        cin >> a >> b >> c;
        if (())
        {
            /* code */
        }
        }

    return 0;
}